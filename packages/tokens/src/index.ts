/**
 * @windforge/tokens — the single token surface.
 *
 * Re-exports the generated barrel (wf* CSS-var constants, the token source maps
 * for reference pages, and accent metadata). The generated CSS lives at
 * `@windforge/tokens/tokens.css`; the Tailwind map at `@windforge/tokens/tailwind`.
 */
export * from './generated'
